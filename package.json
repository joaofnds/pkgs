{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "publish": "lerna publish from-package --yes",
    "version": "lerna version --yes"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
